/**
 * [Lv.2] 124 나라의 숫자 - 복습 템플릿
 *
 * 📌 문제 요약
 * - 1, 2, 4만 사용하는 특수 진법으로 자연수 N을 표현한다.
 * - 0이 존재하지 않기 때문에 나머지가 0일 때 보정이 필요하다.
 *
 * 🔍 핵심 로직
 * 1. (n - 1)을 3으로 나눈 나머지/몫을 활용한다.
 * 2. 나머지가 0일 때는 숫자 4가 들어가고, 몫에서 1을 빼준다.
 * 3. 문자열을 뒤집어 최종 결과를 얻는다.
 *
 * 🎯 복습 목표
 * - while 루프로 몫이 0이 될 때까지 반복문을 돌리는 구조를 직접 구현한다.
 * - 나머지에 따라 digitMap을 선택하고, 결과 문자열 앞에 붙인다.
 * - 입력 1~10 범위에 대해 결과를 확인하며 패턴을 체득한다.
 */

function solution(n: number): string {
  // TODO: 1) while 루프로 n이 0이 될 때까지 반복하세요.
  // TODO: 2) (n - 1) % 3 으로 나머지를 구해 1,2,4 중 하나를 결정하세요.
  // TODO: 3) n을 (n - 1) / 3 으로 갱신하세요. (Math.floor 사용)
  // TODO: 4) 누적한 문자열을 뒤집거나 앞에 붙여 최종 결과를 반환하세요.
  let answer = "";
  while (n > 0) {
    const remainder = n % 3;
    if (remainder === 0) {
      answer += "4";
      n = n / 3 - 1;
    } else {
      answer = remainder + answer;
      n = Math.floor(n / 3);
    }
  }

  return answer;
}

// 🧪 테스트 케이스
const testCases: Array<{ input: number; expected: string }> = [
  { input: 1, expected: "1" },
  { input: 2, expected: "2" },
  { input: 3, expected: "4" },
  { input: 4, expected: "11" },
  { input: 10, expected: "41" },
];

console.log("=== 124 나라의 숫자 복습 ===");
testCases.forEach(({ input, expected }, index) => {
  const result = solution(input);
  console.log(`케이스 ${index + 1}:`, result, "기대값:", expected);
});

export default solution;
