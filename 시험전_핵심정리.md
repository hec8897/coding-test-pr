# 🔥 코딩테스트 시험 직전 핵심 정리

> **마지막 체크! 시험장 가기 전 5분 복습용** ⏰

---

## 🎯 문제 유형별 접근법 (1분 체크)

| 키워드             | 알고리즘     | 자료구조/패턴 |
| ------------------ | ------------ | ------------- |
| **최소/최대**      | 그리디       | 정렬 → 선택   |
| **괄호, 짝맞추기** | -            | 스택          |
| **순서대로 처리**  | -            | 큐            |
| **모든 경우**      | DFS/완전탐색 | 재귀          |
| **빈도수, 중복**   | -            | Map/Set       |
| **k번째, 정렬**    | -            | sort()        |

---

## 🔥 그리디 알고리즘 (2분)

### 핵심 3줄

```
1. 정렬부터 하기
2. 현재 최선 선택
3. 뒤돌아보지 않기
```

### 3가지 패턴

```typescript
// 1. 투 포인터 (구명보트)
arr.sort((a, b) => a - b);
let left = 0,
  right = arr.length - 1;
while (left <= right) {
  /* 양쪽에서 좁혀가기 */
}

// 2. 순회 (체육복)
for (let i = 0; i < n; i++) {
  if (조건) {
    /* 바로 선택 */
  }
}

// 3. 스택 (큰 수 만들기)
while (stack.length > 0 && 조건) {
  stack.pop(); // 제거
}
stack.push(현재); // 추가
```

---

## 📚 스택/큐 (1분)

### 스택 (LIFO)

```typescript
const stack = [];
stack.push(1); // 추가
stack.pop(); // 제거 (마지막)
stack[stack.length - 1]; // top 확인
```

**쓰임**: 괄호, 연속 제거, 이전 값 관리

### 큐 (FIFO)

```typescript
const queue = [];
queue.push(1); // 추가
queue.shift(); // 제거 (첫번째)
```

**쓰임**: 순서대로 처리, 작업 관리

---

## 🌲 재귀/DFS (1분)

### 필수 2요소

```typescript
function dfs(params) {
  // 1. 종료 조건 (필수!)
  if (조건) return;

  // 2. 재귀 호출
  dfs(더_작은_문제);
}
```

### 패턴

```typescript
// DFS: 모든 경우 탐색
function dfs(index, sum) {
  if (index === n) return; // 종료

  dfs(index + 1, sum + arr[index]); // +
  dfs(index + 1, sum - arr[index]); // -
}
```

---

## 🗺️ 해시 (Map/Set) (1분)

```typescript
// Map: 키-값, 빈도수
const map = new Map();
map.set(key, value);
map.get(key);
map.has(key);

// Set: 중복 제거
const set = new Set([1, 1, 2, 3]); // {1, 2, 3}
set.add(value);
set.has(value);
```

---

## 🔢 배열 메서드 (1분)

```typescript
// 정렬
arr.sort((a, b) => a - b);      // 오름차순
arr.sort((a, b) => b - a);      // 내림차순

// 자주 쓰는 것
arr.slice(start, end)           // 복사 (원본 유지)
arr.filter(x => 조건)           // 필터링
arr.map(x => 변환)              // 변환
arr.reduce((acc, cur) => ...)   // 누적
arr.includes(값)                // 포함 여부
arr.indexOf(값)                 // 인덱스 찾기
```

---

## ⚠️ 실수 방지 체크리스트

### 정렬

- [ ] `sort()` 비교 함수 **필수**: `(a, b) => a - b`
- [ ] 숫자 정렬인데 비교 함수 없으면? → 문자열로 정렬됨 (틀림!)

### 배열 인덱스

- [ ] 학생 번호 1부터 → 배열 인덱스 0부터 (`-1` 필수!)
- [ ] `arr[arr.length - 1]` (마지막 요소)

### 재귀

- [ ] 종료 조건 있나?
- [ ] 무한 루프 아닌가?

### 투 포인터

- [ ] `while (left <= right)` (등호 포함!)
- [ ] 정렬 먼저 했나?

### 그리디

- [ ] 정렬부터 했나?
- [ ] 반례 없나?

---

## 🚀 문제 풀이 5단계

```
1️⃣ 제한사항 확인 (n 범위 → 시간복잡도)
2️⃣ 예제 손으로 풀어보기 (패턴 파악)
3️⃣ 알고리즘 선택 (그리디? 완전탐색?)
4️⃣ 구현 (주석으로 단계 나누기)
5️⃣ 테스트 (예제 + 엣지 케이스)
```

---

## 💡 시간 복잡도 (빠른 판단)

| n 크기      | 허용 시간 복잡도 | 알고리즘   |
| ----------- | ---------------- | ---------- |
| n ≤ 100     | O(n²)            | 이중 for문 |
| n ≤ 1,000   | O(n²)            | 이중 for문 |
| n ≤ 10,000  | O(n log n)       | 정렬       |
| n ≤ 100,000 | O(n)             | 단일 반복  |

---

## 🎯 자주 나오는 패턴 TOP 5

### 1. 정렬 + 그리디

```typescript
arr.sort((a, b) => a - b);
// 앞에서/양쪽에서 최선 선택
```

### 2. 스택 + 짝맞추기

```typescript
if (stack.top === "(" && current === ")") {
  stack.pop(); // 짝 제거
}
```

### 3. 재귀 + 완전탐색

```typescript
function dfs(index) {
  if (index === n) return;
  // 모든 경우 시도
}
```

### 4. Map + 빈도수

```typescript
map.set(key, (map.get(key) || 0) + 1);
```

### 5. 투 포인터

```typescript
let left = 0, right = n - 1;
while (left < right) { ... }
```

---

## 🔥 마지막 당부

### ✅ DO

- **정렬 먼저** 생각하기
- **작은 예제**로 손으로 풀어보기
- **엣지 케이스** 확인 (빈 배열, 1개, 같은 값)
- **주석**으로 단계 나누기
- **console.log** 활용해서 디버깅

### ❌ DON'T

- 복잡하게 생각하지 말기
- 완벽한 코드 쓰려 하지 말기
- 한 문제에 너무 오래 안 걸리기 (15분 넘으면 다음 문제)
- `sort()` 비교 함수 빼먹지 말기!

---

## 💪 마인드셋

```
차분하게, 하나씩!
아는 문제부터!
완벽하지 않아도 괜찮아!
```

---

## 🎯 시험 시작 전 체크

- [ ] 손 풀기 (간단한 문제 1개)
- [ ] 정렬 비교함수 암기
- [ ] 종료 조건 잊지 않기
- [ ] 침착하게!

---

# 🔥 화이팅! 잘할 수 있어요! 💪

**Remember**:

- 정렬 먼저!
- 종료 조건!
- 비교 함수!
- 엣지 케이스!

**Go get 'em!** 🚀✨
